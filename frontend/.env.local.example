# URL pública usada desde el navegador (client-side fetch)
NEXT_PUBLIC_API_URL=http://localhost:8000

# URL interna usada por Next.js en el servidor (server components / SSR)
# En desarrollo local, puede ser la misma que NEXT_PUBLIC_API_URL.
API_URL=http://localhost:8000
